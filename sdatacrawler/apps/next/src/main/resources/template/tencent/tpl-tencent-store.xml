<?xml version="1.0"?>
<store>
	<!-- 
	如果有多个collection，配置文件中内部field的子集合一定要放到前面，datum的大集合放到配置文件的最后面
	<collection name="posts" primyKey="_id" >
		<update>reviews</update>
		<remove>user</remove>
	</collection> 
	-->
	<collection name="tencent_users" primaryKey="id" field="userinfo">
	</collection>
	<collection name="tencent_tweets" primaryKey="id" index="true">
		<colfly name="tweet">*</colfly>
		<remove>userinfo</remove>
	</collection>
</store>